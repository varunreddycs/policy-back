packages/ (Phase 2 scaffold)

This folder is for modular Phase 2 components:
- core: shared DTOs, errors, config
- retrieval: retrieval providers (pgsql FTS, pgvector, Azure Search)
- ranking: business ranking + conflict detection
- governance: audit + prompt registry/versioning
- rag: orchestration service

These modules are currently stubs to establish import paths.
