This folder is reserved for prompt templates (Phase 2).

Suggested convention:
- One file per prompt intent, with version suffixes.
- Keep prompts immutable and reference by name+version in the registry.
