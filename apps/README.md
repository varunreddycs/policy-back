apps/ (Phase 2 scaffold)

- apps/api: API entrypoint (uvicorn apps.api.main:app)
- apps/worker: Worker entrypoint (python -m apps.worker.main)

Phase 1 code remains at the repo root (app.py) and worker/.
