Local infra (Phase 2 scaffold)

This folder is intended to hold local development infrastructure such as docker compose, Azurite, and Postgres.

Current Phase 1 files still live at the repo root:
- docker-compose.yml
- Dockerfile
- .env

Migration approach:
- Copy/move docker-compose.yml and related scripts into this folder once you are ready to update docs and CI.
